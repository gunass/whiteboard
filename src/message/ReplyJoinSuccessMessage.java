package message;

/**
 * Used when a client's join request has been approved by an admin
 * Grants access to the remote Whiteboard object
 */
public class ReplyJoinSuccessMessage extends Message {

    public ReplyJoinSuccessMessage() {
        super();
    }
}
